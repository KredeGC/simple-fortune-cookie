version: '3.1'

services:
  backend:
    build: ./backend/
    networks:
      - internal
  frontend:
    build: ./frontend/
    environment:
      - BACKEND_DNS=backend_con
    depends_on:
      - backend
    networks:
      - internal

networks:
  internal: